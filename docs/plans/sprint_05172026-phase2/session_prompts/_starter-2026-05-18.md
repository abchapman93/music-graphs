# Session starter — next PM session (Track L)

**Use:** Paste the body below into a fresh Claude Code session opened in `/Users/alecchapman/Code/music-graphs/`. It boots straight into Track L. If you'd rather do Track J first, swap the section as noted at the bottom.

---

## Prompt to paste

You are the **sprint manager** for the music-graphs project, Phase 2.5. The full handoff for the prior session is at `docs/plans/sprint_05172026-phase2/session_prompts/_pm-continuation.md` — read it in full before doing anything else.

**Current state (one-line summary):** Phase 2.5 planned; Track K (graph view UI) shipped and signed at `f08157e`. Tracks J and L remain. Track M (closeout) blocked on J + L.

**Today's track: L — Wikipedia category as seed source.**

Workflow:

1. Read `_pm-continuation.md` and `_phase-2.5-plan.md` to refresh context.
2. Read the track spec at `docs/plans/sprint_05172026-phase2/session_prompts/L-wikipedia-seeds.md` — it has the full deliverable list and acceptance gates (L1, L2, L3).
3. Open a worktree with `EnterWorktree` named `mg-L-wikipedia-seeds`. **All edits must use paths inside the worktree** — verify `pwd` before any Edit/Write call. (Last session got bitten by absolute paths leaking edits into the main repo. Don't repeat.)
4. Implement:
   - New skill `skills/retrieve-wikipedia-category/` (mirror `skills/retrieve-spotify-song/` shape).
     - Input: Wikipedia Category URL.
     - Output: `[{title, url}, ...]` via MediaWiki API `?action=query&list=categorymembers&cmtitle=Category:...&format=json` (with pagination via `continue`).
     - Fail-loud on 404, non-category URL, or empty category. Same contract as `retrieve-spotify-song`.
     - Tests: mock HTTP; cover pagination + error paths.
   - `expand-graph` enhancement: read `seed_sources:` from the graph root card frontmatter; for each Wikipedia URL, call `retrieve-wikipedia-category`, dedupe against existing graph slugs, and surface top-N new candidates **alongside** existing Spotify-MCP candidates (don't replace — augment, and label the source).
   - Update root cards:
     - `graphs/bowie-covers/cards/README.md`: add `seed_sources: [Category:Songs_written_by_David_Bowie]`
     - `graphs/pittsburgh-jazz/cards/README.md`: add `seed_sources: [Category:Jazz_musicians_from_Pittsburgh]`
     - `graphs/band-x/cards/README.md`: **ask Alec for a category URL.** If he has none, sign L3 as "n/a for band-x" and move on.
   - Document the seed-source pattern in `docs/skills-overview.md` (or wherever the skill catalog lives).
5. Gate: 100+ pytest pass, lint clean across all 3 graphs.
6. Commit, ff-merge to main, tear down worktree.
7. Sign L1/L2/L3 + Z3/Z4 in `test_registry.md`. Surface M14 to Alec to test in a separate `expand-graph` run.
8. Overwrite `_pm-continuation.md` with a fresh handoff before ending.

**Open blocking question to surface to Alec early in the session:** what Wikipedia category URL (if any) should be the seed for the band-x graph? Don't block the rest of Track L on this — ask while implementing.

**Key decisions you must not re-litigate** (from the handoff):
- Wikipedia API, not HTML scraping.
- Augment Spotify-MCP candidates, don't replace them.
- Intake storage is per-graph (already decided for Track J — affects nothing in L but worth knowing).

**Main branch HEAD at session start:** `da36b08 pm: overwrite continuation with handoff for next PM session`. Verify with `git log --oneline -1` before opening the worktree.

When you're ready, propose the first concrete action and ask me to confirm.

---

## If swapping to Track J instead

Replace the "Today's track" line and section 4 with the deliverables from `J-suggestion-intake.md`:
- `graphs/{slug}/inbox/` folders
- `skills/triage-suggestion/` skill (CSV row → present → `add-node`/`add-edge` reuse)
- `docs/family-collaboration.md`
- Fix `write_card.py` `repo_root` inference bug (it will trip on `triage-suggestion`)

Acceptance gates: J1, J2 automated + M11, M12 manual.
